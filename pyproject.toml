[project]
name = "skills"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pybadges",
    "skills-ref>=0.1.1",
]

[tool.uv.sources]
pybadges = { git = "https://github.com/NicholasSynovic/pybadges" }
